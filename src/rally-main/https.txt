🔧 En local : FastAPI en HTTPS avec un certificat auto-signé
✅ Étapes :
1. Génère un certificat SSL auto-signé (avec mkcert)
Si tu n'as pas encore mkcert, installe-le :

bash
Copy
Edit
brew install mkcert           # sur macOS
choco install mkcert          # sur Windows
sudo apt install libnss3-tools && curl -JLO https://dl.filippo.io/mkcert/latest?for=linux/amd64 && chmod +x mkcert && sudo mv mkcert /usr/local/bin  # Linux
Puis crée les certificats :

bash
Copy
Edit
mkcert -install
mkcert 127.0.0.1
Tu obtiendras :

127.0.0.1.pem (certificat)

127.0.0.1-key.pem (clé privée)

Déplace-les dans ton dossier back/ ou un dossier certs/.

2. Lancer FastAPI avec HTTPS

# back/main.py
stAPI()

if __name__ == "__main__":
    uvicorn.run("main:app", host="0.0.0.0", port=8000,
                ssl_certfile="certs/localhost.pem",
                ssl_keyfile="certs/localhost-key.pem")
Ton backend sera accessible à https://localhost:8000.

